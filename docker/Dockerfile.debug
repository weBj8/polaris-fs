FROM plfs-rust:4.59.2

RUN apt-get update \
 && apt-get install -y --no-install-recommends gdb \
 && rm -rf /var/lib/apt/lists/*

# unstripped binary for symbolized backtraces
COPY target/release/plfsmount /usr/local/sbin/plfsmount
